result: 50/100

### 1. Краткое описание задания
Задание включает создание трёх функций:
1. `rectangle_info(width, height)` — возвращает кортеж `(площадь, периметр)` прямоугольника.
2. `is_adult(age)` — возвращает `True`, если возраст ≥ 18, иначе `False`.
3. `safe_div(a, b)` — возвращает `a / b`, если `b ≠ 0`, иначе `None`.

**Ключевые требования**: 
- Типы возвращаемых значений должны строго соответствовать условию (кортеж, булево значение, число/`None`).
- Обработка крайних случаев (деление на ноль).

---

### 2. Результаты проверки
**Файл**: `BG37-1.py`

#### Тестирование функций:
1. **rectangle_info(3, 5)**:
   - Ожидаемый вывод: `(15, 16)`.
   - Фактический вывод: `(15, 16)` — корректно.

2. **is_adult(23)**:
   - Ожидаемый вывод: `True` (тип `bool`).
   - Фактический вывод: `"True"` (тип `str`) — ошибка.

3. **safe_div(6, 3)**:
   - Ожидаемый вывод: `2.0` (тип `float`).
   - Фактический вывод: `2.0` — корректно.
   - **Дополнительный тест**: `safe_div(5, 0)` → ожидается `None`, но возвращается `"None"` (тип `str`).

---

### 3. Сильные стороны работы
- **Корректность**: Функция `rectangle_info` реализована верно.
- **Читаемость**: Код структурирован, переменные названы понятно (кроме `square` вместо `area`).
- **Обработка условий**: В `safe_div` корректно проверяется деление на ноль.

---

### 4. Ошибки и рекомендации

#### **Значимые ошибки** (снижение на 40 баллов):
1. **Неверный тип возвращаемого значения в `is_adult`**:
   - **Код**: `return "True" if age >= 18 else "False"`.
   - **Проблема**: Возвращаются строки вместо булевых значений.
   - **Исправление**:
     ```python
     def is_adult(age):
         return age >= 18
     ```

2. **Неверный тип возвращаемого значения в `safe_div` при делении на ноль**:
   - **Код**: `result = "None" if b == 0 else a / b`.
   - **Проблема**: Возвращается строка `"None"` вместо объекта `None`.
   - **Исправление**:
     ```python
     def safe_div(a, b):
         return None if b == 0 else a / b
     ```

#### **Минорные недочёты** (снижение на 10 баллов):
- **Избыточный код**: Вызовы `print()` и присваивание результатов переменным (`rect`, `result`) не требуются по заданию. Их следует удалить.

---

### 5. Оценка
- **Функциональность (50%)**: 25/50  
  Две функции из трёх работают некорректно из-за неверных типов.
- **Качество кода (30%)**: 20/30  
  Логика функций в целом верна, но есть избыточный код.
- **Стиль и тесты (20%)**: 15/20  
  Отсутствуют тесты, но читаемость приемлемая.

**Итог**: 50/100 баллов.

---

### 6. Вариант полного решения
```python
def rectangle_info(width, height):
    area = width * height
    perimeter = 2 * (width + height)
    return (area, perimeter)

def is_adult(age):
    return age >= 18

def safe_div(a, b):
    if b == 0:
        return None
    return a / b
```

---

Анализ выполнен моделью: GPT-4o.
