Время затраченное на выполнение: 0:09

result: 33/100

1) **Сильные стороны**
- Решение корректно выполняет все три запроса первого уровня (базовый).
- Запросы написаны в правильном синтаксисе SQL, используются верные ключевые слова (SELECT, FROM, WHERE).
- В третьем запросе правильно применено логическое условие с оператором AND.

2) **Ошибки и недочёты (сгруппировать по серьёзности)**

**Блокирующие (ломает выполнение требований задания)**
- Решение содержит только запросы для Уровня 1 (Базовый). Полностью отсутствуют запросы для Уровня 2 (Средний) и Уровня 3 (Продвинутый). Это прямое невыполнение большей части условий задания.

**Значимые (может дать неверный результат на части кейсов, сильно ухудшает качество)**
- Во втором запросе (`sELECT Name from titanic WHERE age>30;`) используется строчная буква 's' в ключевом слове `SELECT`. Хотя многие СУБД нечувствительны к регистру ключевых слов, это считается плохой практикой и может привести к ошибкам в некоторых окружениях. Следует писать `SELECT`.

**Минорные (стиль, читаемость, мелкие улучшения без влияния на правильность)**
- Отсутствует форматирование и разделение запросов. Все три запроса записаны в одну строку, что ухудшает читаемость. Рекомендуется разделять их пустыми строками или комментариями.
- В первом запросе (`SELECT * from titanic;`) после `SELECT` и перед `*` рекомендуется ставить пробел для лучшей читаемости.
- Имя таблицы `titanic` в условии указано в условии, но в реальной базе имя может быть другим (например, с кавычками или в другом регистре). Однако, так как в задании явно указана таблица `titanic`, это не является ошибкой.

3) **Оценка и как она посчитана**
Используем веса:
- Функциональность и соответствие условию: 15/50 (Выполнены только 3 из 9 требуемых запросов. За каждый запрос можно условно начислить ~5.55 балла (50/9). 3 запроса = ~16.67, но с учетом блокирующей ошибки (неполное выполнение) оценка снижена до 15).
- Качество кода (структура, читаемость, устойчивость, отсутствие дублирования): 10/30 (Код минимально рабочий для своей части, но есть стилевые недочёты и одна значимая ошибка с регистром).
- Стиль и тесты: 8/20 (Тесты не требовались и не предоставлены. Стиль базовый, но есть проблемы с форматированием).

Итог: 15 + 10 + 8 = 33 балла.

4) **Если задание выполнено не полностью**
- **Отсутствует полностью:** 6 SQL-запросов (Уровень 2: задачи 1, 2, 3; Уровень 3: задачи 1, 2, 3).
- **Выполнено частично:** Уровень 1 выполнен полностью.

**Вариант полного решения (дополнение к коду студента):**

```sql
-- Уровень 1: Базовый (уже выполнено студентом)
SELECT * from titanic;
SELECT Name from titanic WHERE age>30;
SELECT Name from titanic WHERE sex='female' and Pclass=1;

-- Уровень 2: Средний
-- 1. Имена и возраст выживших пассажиров, отсортированные по возрасту
SELECT Name, Age from titanic WHERE Survived = 1 ORDER BY Age;

-- 2. Имена пассажиров, которые путешествовали одни
SELECT Name from titanic WHERE SibSp = 0 AND Parch = 0;

-- 3. Имена и классы пассажиров, стоимость билета которых превышала 100
SELECT Name, Pclass from titanic WHERE Fare > 100;

-- Уровень 3: Продвинутый
-- 1. Пассажиры (имя, класс, возраст), которые путешествовали не первым классом и были старше 18 лет
SELECT Name, Pclass, Age from titanic WHERE Pclass != 1 AND Age > 18;

-- 2. Пассажиры, которые не выжили и у которых не было родственников на борту
SELECT * from titanic WHERE Survived = 0 AND SibSp = 0 AND Parch = 0;

-- 3. Имена, стоимость билетов и класс мест тех пассажиров, которые заплатили за билет меньше 10, но путешествовали не третьим классом
SELECT Name, Fare, Pclass from titanic WHERE Fare < 10 AND Pclass != 3;
```

**Примечание:** В приведенном решении предполагаются имена столбцов, характерные для популярного датасета Titanic (`Survived`, `SibSp`, `Parch`, `Fare`). Если в реальной таблице имена столбцов отличаются, их необходимо заменить согласно схеме базы данных.
