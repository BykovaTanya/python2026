Время затраченное на выполнение: 0:36

result: 50/100

1) **Сильные стороны**
- Все запросы написаны в правильном синтаксисе SQL.
- Имена столбцов и таблицы соответствуют ожидаемым (titanic, Name, Age, Sex, Pclass, Survived, Fare, Parch, SibSp).
- Запросы для задач 1, 2, 3 первого уровня, задачи 1 и 3 второго уровня, задачи 1 третьего уровня в целом корректны.
- Студент пытался применить сортировку и условия.

2) **Ошибки и недочёты (сгруппировать по серьёзности)**

**Блокирующие (ломает выполнение требований задания)**
- Задача 2 (Уровень 2): "Выведите имена пассажиров, которые путешествовали одни (без братьев, сестёр, супруга или супруги, родителей или детей)". В решении: `SELECT Name from titanic WHERE Parch=0 or SibSp=0;`. Это условие `OR` означает, что будет выбран пассажир, у которого **хотя бы один** из счетчиков родственников равен нулю. Это неверно. Требуется, чтобы **оба** счетчика были равны нулю (нет ни супругов/братьев-сестер, ни родителей/детей). Правильное условие: `WHERE Parch = 0 AND SibSp = 0`.
- Задача 2 (Уровень 3): "Выведите список пассажиров, которые не выжили и у которых не было родственников на борту". В решении: `SELECT Name from titanic WHERE Survived=0 and Parch=0 or SibSp=0;`. Из-за приоритета операторов (`AND` выполняется перед `OR`) этот запрос интерпретируется как `WHERE (Survived=0 AND Parch=0) OR SibSp=0`. Это возвращает: 1) всех невыживших без родителей/детей, **а также** 2) всех, у кого нет братьев/сестер/супругов, независимо от выживания. Это полностью не соответствует условию. Нужно: `WHERE Survived = 0 AND Parch = 0 AND SibSp = 0`. Также, согласно примеру псевдокода, возможно, требовалось выбрать все колонки (`SELECT *`), а не только имена.
- Задача 3 (Уровень 3): "Выведите имена, стоимость билетов и класс мест тех пассажиров, которые заплатили за билет меньше 10, но путешествовали не третьим классом". В решении: `SELECT Name,fare, Pclass from titanic WHERE fare<10 ORDER by Pclass !=3;`. Здесь `ORDER by Pclass !=3` — это попытка использовать условие в `ORDER BY`, что не является фильтрацией. Условие "не третьим классом" должно быть частью `WHERE`: `WHERE fare < 10 AND Pclass != 3`. Сортировка в условии не требуется.

**Значимые (может дать неверный результат на части кейсов, сильно ухудшает качество)**
- Задача 1 (Уровень 2): "Выведите имена и возраст выживших пассажиров, используя сортировку по возрасту". В решении: `SELECT Name,Age from titanic WHERE Survived=1 ORDER by age;`. Запрос корректный, но не указано направление сортировки. В примере псевдокода указано "по возрастанию". Хотя это часто подразумевается по умолчанию, явное указание `ORDER BY age ASC` было бы лучше для однозначности. Не является критической ошибкой, но стоит отметить.
- В нескольких строках SQL используется разный регистр для ключевых слов (например, `sELECT` во второй строке). Хотя это не влияет на выполнение в большинстве СУБД, это нарушает единообразие и может считаться неаккуратным.

**Минорные (стиль, читаемость, мелкие улучшения без влияния на правильность)**
- Отсутствует форматирование: все запросы идут в одну строку каждый, что ухудшает читаемость. Рекомендуется переносить ключевые слова и условия на новые строки.
- В последнем запросе (`ORDER by Pclass !=3`) пробел между `ORDER` и `by` отсутствует, хотя `ORDER BY` является единым ключевым словом. Лучше писать `ORDER BY`.

3) **Оценка и как она посчитана**

**Функциональность и соответствие условию: 25/50**
- Уровень 1: 3 задачи из 3 выполнены корректно. (+15 баллов, условно 5 за каждую)
- Уровень 2: Задача 1 выполнена с мелким замечанием. Задача 2 выполнена неверно (блокирующая ошибка). Задача 3 выполнена корректно. (+10 баллов: 5+0+5)
- Уровень 3: Задача 1 выполнена корректно. Задача 2 выполнена неверно (блокирующая ошибка). Задача 3 выполнена неверно (блокирующая ошибка, условие фильтрации не соблюдено). (+5 баллов: 5+0+0)
Итого: 15+10+5 = 30. Минус 5 за общие недочеты (несоответствие по сортировке в задаче 2 уровня 2, возможное несоответствие вывода всех колонок в задаче 2 уровня 3) = 25.

**Качество кода (структура, читаемость, устойчивость, отсутствие дублирования): 15/30**
- Структура: запросы перечислены подряд без разделения комментариями или пустыми строками, что затрудняет навигацию. (-5)
- Читаемость: плохое форматирование, смешанный регистр ключевых слов. (-5)
- Устойчивость: логические ошибки в условиях (OR вместо AND, неправильный приоритет операторов) показывают недостаточное понимание, что критично. (-5)

**Стиль и тесты: 10/20**
- Стиль: неаккуратный код, отсутствие единообразия. (-10)
- Тесты не требовались и не предоставлены.

**Итог: 25 + 15 + 10 = 50/100**

4) **Если задание выполнено не полностью**
Недореализованные части:
- Задача 2 (Уровень 2): условие для "путешествовали одни" реализовано неверно.
- Задача 2 (Уровень 3): условие для "не выжили и у которых не было родственников" реализовано неверно; возможно, также неверный набор выбираемых колонок.
- Задача 3 (Уровень 3): условие фильтрации "не третьим классом" отсутствует в WHERE, вместо этого некорректно использовано в ORDER BY.

Вариант исправленного полного решения (только исправленные запросы):

```sql
-- Уровень 1 (уже корректны)
SELECT * FROM titanic;
SELECT Name FROM titanic WHERE Age > 30;
SELECT Name FROM titanic WHERE Sex = 'female' AND Pclass = 1;

-- Уровень 2
-- Задача 1
SELECT Name, Age FROM titanic WHERE Survived = 1 ORDER BY Age ASC;
-- Задача 2 (исправлено)
SELECT Name FROM titanic WHERE Parch = 0 AND SibSp = 0;
-- Задача 3
SELECT Name, Pclass FROM titanic WHERE Fare > 100;

-- Уровень 3
-- Задача 1
SELECT Name, Pclass, Age FROM titanic WHERE Pclass != 1 AND Age > 18;
-- Задача 2 (исправлено: все колонки и правильное условие)
SELECT * FROM titanic WHERE Survived = 0 AND Parch = 0 AND SibSp = 0;
-- Задача 3 (исправлено: убрана сортировка, добавлено условие)
SELECT Name, Fare, Pclass FROM titanic WHERE Fare < 10 AND Pclass != 3;
```
