Вопросы с выбором варианта: 78/78%  
Вопросы с текстовыми ответами: 22/22%  
# Вопрос 1. Блок 1. Предварительная подготовка
  
### Что нужно сделать до всех заданий

1. Создай папку проекта.
2. Создай файлы:
   - `index.html`
   - `styles.css`
3. Добавь базовую структуру HTML (каркас документа).
4. Подключи CSS через `link`.
5. Выбери тему страницы. Это будет “страница-визитка” с секциями и карточками.

### Правила (обязательные)

- Стили пишем только в `styles.css`.
- В HTML используем семантические теги там, где это уместно.
- Имена классов пишем латиницей.
- В конце занятия должна получиться одна цельная страница, а не набор кусочков.

### Как правильно пользоваться ИИ (чтобы это было обучение)

- Не отправляй в ИИ “сделай красиво”. Отправляй конкретные условия.
- Не копируй результат вслепую. Сначала прочитай и пойми структуру.
- После ИИ обязательно внести изменения по доп. условиям. Это часть сдачи.

---

## Блок 2. Каркас HTML и семантика

### Содержание и вопросы

- Что такое базовая структура HTML-документа?
- Зачем нужны `head` и `body`?
- Какие семантические теги подходят для шапки, основного контента и подвала?
- Как правильно использовать заголовки `h1`-`h3`?

### Материал (лаконично)

- `<!doctype html>` это “режим HTML”. Без него браузер может странно интерпретировать разметку.
- `head` это “настройки страницы”:
  - кодировка, чтобы текст на русском не ломался
  - `viewport`, чтобы страница нормально выглядела на телефоне
  - `title`, чтобы было понятное имя вкладки
  - подключение `styles.css`
- `body` это все, что видит пользователь.
- Семантические теги это понятные названия блоков вместо безликих контейнеров. Они помогают:
  - быстрее ориентироваться в коде
  - лучше понимать структуру страницы
  - подготовить основу для доступности
- Минимальный набор для страницы-визитки: `header` (шапка), `main` (основное), `footer` (подвал).
- Заголовки:
  - `h1` один на страницу, это главный смысл страницы
  - `h2` для секций
  - `h3` для элементов внутри секции, например для карточек
  
Варианты ответов:
1) ✅ Прочитано
  
### ИИ анализ
Это не вопрос, а подтверждение ознакомления с материалом. Отметка "Прочитано" означает, что студент изучил правила и теоретическую часть перед выполнением практических заданий. Это важный шаг для успешного выполнения работы.

#  Вопрос 2. Практическое задание (Блок 2)
  
### Условия

Сделай каркас `index.html` пошагово:

1. Создай базовую структуру документа.
2. В теге `html` установи язык документа.
3. В `head` добавь:
   - кодировку
   - настройку `viewport`
   - заголовок вкладки
   - подключение `styles.css`
4. В `body` создай 3 блока:
   - шапка
   - основная часть
   - подвал
5. В шапке добавь:
   - главный заголовок страницы
   - короткую строку-описание
6. В основной части создай минимум 3 секции.
7. В каждой секции добавь заголовок.

Проверка:

- Главный заголовок один.
- Секций в основной части не меньше трех.

### Псевдокод решения

```text
CREATE HTML_DOCUMENT
  SET DOCUMENT_LANGUAGE
  ADD META_CHARSET
  ADD META_VIEWPORT
  SET PAGE_TITLE
  LINK STYLESHEET_FILE

  BODY
    HEADER
      MAIN_TITLE
      SUBTITLE

    MAIN
      SECTION_A
        SECTION_TITLE
        PARAGRAPH

      SECTION_B
        SECTION_TITLE
        CONTENT_PLACEHOLDER

      SECTION_C
        SECTION_TITLE
        CONTENT_PLACEHOLDER

    FOOTER
      CONTACTS_PLACEHOLDER
END
```

### Если использовал ИИ, обязательные изменения вручную

- Переименуй минимум 2 семантических блока (например, добавь `nav` или обертку-контейнер), сохранив смысл.
- Проверь, что заголовки идут по уровням без пропусков.

---

**Вставь решение в комментарий ниже:**
  
### 🟡 Ответ
<!DOCTYPE html>  
<html lang="ru">  
<head>  
    <meta charset="UTF-8">  
    <meta name="viewport" content="width=device-width, initial-scale=1.0">  
    <title>Моя Визитка</title>  
    <link rel="stylesheet" href="styles.css">  
</head>  
<body>  
  
    <header>  
        <h1>Главный заголовок страницы</h1>  
        <p>Короткое описание.</p>  
    </header>  
  
    <main>  
        <section>  
            <h2>О проекте</h2>  
            <!-- Здесь будет ваш текст -->  
        </section>  
  
        <section>  
            <h2>Наши услуги</h2>  
            <!-- Здесь будет ваш текст -->  
        </section>  
  
        <section>  
            <h2>Контакты</h2>  
            <!-- Здесь будет ваш текст -->  
        </section>  
    </main>  
  
    </footer>  
        <p>&copy; Татьяна </p>  
    </footer>  
  
</body>  
</html>  
  
### ИИ анализ
Ответ в целом правильный и соответствует основным требованиям задания: есть базовая структура, язык, мета-теги, заголовок, подключение CSS, семантические блоки `header`, `main`, `footer` и три секции с заголовками `h2`.  
Однако есть синтаксическая ошибка: закрывающий тег `</footer>` написан как открывающий (`</footer>` вместо `<footer>`). Также не выполнено дополнительное условие о переименовании семантических блоков (например, добавление `nav` или контейнера). Для улучшения стоит исправить ошибку и добавить больше смысловых элементов.

# Вопрос 3. Где находится видимый пользователю контент страницы?
  
  
Варианты ответов:
1) ✅ Внутри `body`
2) Внутри `head`
3) Внутри `link`
4) Внутри `meta`
5) Внутри `title`
  
### ИИ анализ
Правильный ответ — внутри `body`. Тег `body` содержит всё содержимое веб-страницы, которое отображается в окне браузера: текст, изображения, ссылки и другие элементы. Остальные варианты (`head`, `link`, `meta`, `title`) служат для метаданных, настроек и подключения ресурсов, которые не отображаются напрямую пользователю.

# Вопрос 4. Для чего обычно нужен `meta viewport`?
  
  
Варианты ответов:
1) ✅ Для нормального вида на телефоне
2) Для настройки шрифтов
3) Для создания списка
4) Для включения JavaScript
5) Для вставки картинки
  
### ИИ анализ
Правильный ответ — для нормального вида на телефоне. Метатег `viewport` управляет макетом и масштабированием на мобильных устройствах, обеспечивая корректное отображение страницы без необходимости горизонтальной прокрутки. Остальные задачи решаются другими средствами: шрифты настраиваются в CSS, списки создаются тегами `<ul>`/`<ol>`, JavaScript подключается отдельно, а картинки вставляются тегом `<img>`.

# Вопрос 5. Какой набор тегов лучше описывает структуру страницы-визитки?
  
  
Варианты ответов:
1) `b`, `i`, `u`
2) ✅ `header`, `main`, `footer`
3) `form`, `input`, `button`
4) `script`, `style`, `link`
5) `table`, `tr`, `td`
  
### ИИ анализ
Правильный ответ — `header`, `main`, `footer`. Эти семантические теги идеально описывают основные структурные блоки страницы-визитки: шапку, основное содержание и подвал. Остальные наборы предназначены для других целей: первый — для форматирования текста, третий — для форм, четвёртый — для скриптов и стилей, пятый — для табличных данных.

# Вопрос 6. Как правильно использовать заголовок `h1` на странице?
  
  
Варианты ответов:
1) ✅ Использовать один раз для всей страницы
2) Не использовать никогда
3) Использовать в каждой секции
4) Использовать только в `footer`
5) Использовать вместо текста в `p`
  
### ИИ анализ
Правильный ответ — использовать один раз для всей страницы. Заголовок `h1` должен обозначать главную тему или название всей страницы, что важно для семантики и доступности. Его многократное использование размывает структуру документа, а варианты 2, 4 и 5 противоречат стандартам HTML и логике построения контента.

# Вопрос 7. Для чего нужны семантические теги?
  
  
Варианты ответов:
1) Чтобы ускорить интернет
2) ✅ Чтобы структура была понятнее
3) Чтобы заменить CSS
4) Чтобы скрыть контент
5) Чтобы отключить отступы
  
### ИИ анализ
Правильный ответ — чтобы структура была понятнее. Семантические теги (например, `header`, `article`, `nav`) придают смысл разным частям HTML-документа, делая код более читаемым для разработчиков и понятным для браузеров и вспомогательных технологий. Они не влияют на скорость интернета, не заменяют CSS и не используются для визуального скрытия контента или управления отступами.

# Вопрос 8. Где правильнее подключать CSS-файл?
  
  
Варианты ответов:
1) В `body` после `footer`
2) Внутри `meta charset`
3) Внутри `title` страницы
4) Внутри комментария
5) ✅ В `head` через `link`
  
### ИИ анализ
Правильный ответ — в `head` через `link`. Это стандартное и рекомендуемое место для подключения внешних таблиц стилей, так как оно обеспечивает загрузку CSS до отображения содержимого страницы, предотвращая мерцание нестилизованного контента. Подключение в `body` или внутри других тегов может привести к непредсказуемому поведению и нарушению порядка загрузки ресурсов.

# Вопрос 9. Зачем добавлять `lang` в теге `html`?
  
  
Варианты ответов:
1) Чтобы поменять цвет фона
2) Чтобы отключить ссылки
3) Чтобы включить flexbox
4) Чтобы увеличить заголовки
5) ✅ Чтобы указать язык документа
  
### ИИ анализ
Правильный ответ — чтобы указать язык документа. Атрибут `lang` (например, `lang="ru"`) сообщает браузерам, поисковым системам и программам чтения с экрана, на каком языке написан основной текст страницы. Это важно для корректного отображения символов, автоматического перевода, синтеза речи и SEO. Он не влияет на визуальное оформление или поведение элементов страницы.
